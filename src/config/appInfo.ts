/**
 * Static app identity used in the autosave record and the CSV export header.
 * Keep `APP_VERSION` in step with package.json; `STUDY_ID` is "local" until the
 * cloud/authoring phases assign real study ids.
 */
export const APP_VERSION = '0.1.0';
export const STUDY_ID = 'local';
